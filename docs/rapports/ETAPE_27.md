# BILAN ÉTAPE 27 : cadrage complet des engins

Date : 2026-09-22

## Réalisation

Les photographies de la page Ressources sont affichées dans un cadre 16:9 qui conserve toute l’image. Le recadrage qui coupait une partie des engins a été supprimé.

Chaque photographie reste optimisée localement au format WebP et les crédits affichés sous l’image restent inchangés.

## Vérifications

- `npm.cmd run lint` : réussi ;
- `npm.cmd run typecheck` : réussi ;
- test ciblé de la page Ressources : réussi ;
- `npm.cmd run build` : réussi, 36 routes statiques.

Fin de l’ÉTAPE 27. STOP.
